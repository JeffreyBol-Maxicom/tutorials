<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="estate_property_view_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties" 
                decoration-success="state in ['offer_received', 'offer_accepted']" 
                decoration-bf="state in ['offer_accepted']"
                decoration-muted="state in ['sold']"
                >
                <field name="name" />
                <field name="property_type_id" />
                <field name="postcode" />
                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                <field name="bedrooms" />
                <field name="living_area" />
                <field name='expected_price' />
                <field name="selling_price" />
                <field name="date_availability" optional="hide"/>
            </list>
        </field>
    </record>
    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_sold"
                        class="btn btn-primary"
                        type="object"
                        string="Sold"
                        invisible="state in ['sold', 'cancelled']" />
                    <button name="action_cancel"
                        class="btn btn-outline-info"
                        type="object"
                        string="cancel"
                        invisible="state in ['sold', 'cancelled']" />
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold" options="{'clickable': '1'}"/>
                </header>
                <sheet>
                    <widget name="web_ribbon" title="Cancelled" bg_color="text-bg-danger" invisible="state not in ['cancelled']"/>
                    <div class="oe_title mb8">
                        <h1 class="o_row">
                            <field name="name" />
                        </h1>
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" />
                    </div>
                    <separator />
                    <group>
                        <group>
                            <field name="postcode" />
                            <field name="date_availability" />
                            <field name="property_type_id" options="{'no_create': True}"/>
                        </group>
                        <group>
                            <field name="expected_price" />
                            <field name="best_offer" />
                            <field name="selling_price" />
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description" />
                                <field name="bedrooms" />
                                <field name="living_area" />
                                <field name="garage" />
                                <field name="garden" />
                                <field name="garden_area" invisible="not garden" />
                                <field name="garden_orientation" invisible="not garden" />
                                <field name="total_area" />
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids" readonly="state in ['offer_accepted', 'sold', 'cancelled']"/>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="user_id" />
                                <field name="partner_id" />
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
    <record id="estate_property_view_kanban" model="ir.ui.view">
        <field name="name">estate.property.view.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id">
                <field name="state"/>
                <templates>
                    <t t-name="card">
                        <div class="row mb4 h5">
                            <div class="col-8">
                                <field name="name"/>
                            </div>
                        </div>
                        <div class="row mb4">
                            <div class="col-8">
                                Expected Price: 
                                <field name="expected_price"/>
                            </div>
                        </div>
                        <div class="row mb4">
                            <div t-if="record.state.raw_value == 'offer_received'" class="col-8">
                                Best Offer: 
                                <field name="best_offer" widget="monetary"/>
                            </div>
                            <div t-elif="record.state.raw_value == 'offer_accepted'" class="col-8">
                                Selling Price: 
                                <field name="selling_price" widget="monetary"/>
                            </div>
                        </div>
                        <div class="row mb4">
                            <div class="col-8">
                                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" />
                <field name="postcode" />
                <field name="expected_price" />
                <field name="bedrooms" />
                <field name="living_area" 
                    filter_domain="[('living_area', '>=', self)]"/>
                <field name="facades" />
                <filter name="available" string="Available"
                    domain="[('state','in',('new','offer_received'))]" />
                <filter name="groupby_postcode" string="Postcode" context="{'group_by': 'postcode'}" />
            </search>
        </field>
    </record>

    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="context">{'search_default_available': True}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first property
            </p>
        </field>
    </record>
</odoo>
